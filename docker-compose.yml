version: "3.9"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: development
      LOG_LEVEL: INFO
      SCORE_THRESHOLD: 0.5
    command: ["python", "-m", "src.api.app"]
